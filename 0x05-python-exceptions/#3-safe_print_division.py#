#!/usr/bin/python3
""" Joshua's code """""def safe_print_division(a, b):
    try:
        result = a/b
    except ZeroDivisionError:
        result = None
    finally:
        print("The result is: {}".format(result))
    return (result)